#include <stdio.h>
#include <stdlib.h>
/**
 * main - initiation to c
 * Return: the return is 0
 */
int main(void)
{
	int a;

	while (a < 58)
	{
	putchar(a);
	a++;
	}
	for (a = 48; a < 54; a++)
	{
	putchar(a + 49)
	}
	putchar('\n');
	return (0);
}
