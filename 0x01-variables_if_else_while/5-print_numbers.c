#include <stdio.h>
#include <stdlib.h>
/**
 * main - initiation to C
 * Return: the return is 0
 */
int main(void)
{
	int  x = 0;

	while (x < 10)
	{
	printf("%d", x);
	x++;
	}
	putchar('\n');
	return (0);
}
