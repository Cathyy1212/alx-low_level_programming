#include <stdio.h>
#include <stdlib.h>
/**
 * main - initiation to c
 * Return: the returning is 0
 */
int main(void)
{
	char b = 'a';

	while (b <= 'z')
	{
	if (b! = 'e' && b! = 'q')
	{
	putchar(b);
	b++;
	}
	else
	{
	b++;
	}
	}
	putchar('\n');
	return (0);
}
