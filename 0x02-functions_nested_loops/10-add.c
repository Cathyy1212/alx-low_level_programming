#include <stdio.h>
#include "main.h"/**
 * main - check the code
 * @c: describe the
 * Return: returning 0
 */

int main(void) {
	int result = add(a, b);
	printf("Result: %d\n", result);
	return 0;
}
